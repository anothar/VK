<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    x:Name="mainwindow"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:vkProject.Controls"
        xmlns:Controls="clr-namespace:VkAPI.Controls"
    x:Class="vkProject.MainWindow"
        mc:Ignorable="d"
        Title="vkProject" Height="400" Width="600" MinHeight="300" MinWidth="300"
        Loaded="Window_Loaded"
        Closing="mainwindow_Closing">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="tabs" Grid.Column="0">
            <Controls:ctrTabElement x:Name="tb_posts" Text="Стена" Checked="True" MouseLeftButtonUp="tb_posts_MouseLeftButtonUp"/>
            <Controls:ctrTabElement x:Name="tb_stat" Text="Статистика" MouseLeftButtonUp="tb_stat_MouseLeftButtonUp"/>
        </StackPanel>
        <ScrollViewer x:Name="postViewer" Grid.Column="1" Visibility="Visible" Background="#EEEE">
            <StackPanel Margin="5, 5, 5, 5">
                <StackPanel x:Name="postButton">
                </StackPanel>
                 <StackPanel x:Name="posts">
                    <TextBlock HorizontalAlignment="Center" x:Name="countPost"  Text="0/0" Visibility="Hidden"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <ScrollViewer Grid.Column="1" x:Name="statViewer" Visibility="Hidden" Background="#EEEE">
            <StackPanel x:Name="statistic">
                <StackPanel x:Name="statButtons">
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>